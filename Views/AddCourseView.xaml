<Window x:Class="UniversityModel.Views.AddCourseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityModel.Views"
        mc:Ignorable="d"
        Title="AddCourseView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">

            <TextBlock Text="Course Name"/>
            <TextBox Text="{Binding NewCourse.Name}" />

            <TextBlock Text="Teacher" Margin="0,10,0,0"/>

            <ComboBox ItemsSource="{Binding Teachers}"
                      SelectedItem="{Binding SelectedTeacher}"
                      DisplayMemberPath="FirstName"/>

            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Margin="0,10,0,0"/>

        </StackPanel>
    </Grid>
</Window>
