<Window x:Class="UniversityModel.Views.EditStudentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityModel.Views"
        mc:Ignorable="d"
        Title="EditStudentView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">

            <TextBlock Text="First Name"/>
            <TextBox Text="{Binding EditingStudent.FirstName, Mode=TwoWay}"/>

            <TextBlock Text="Last Name" Margin="0,10,0,0"/>
            <TextBox Text="{Binding EditingStudent.LastName, Mode=TwoWay}"/>

            <TextBlock Text="Age" Margin="0,10,0,0"/>
            <TextBox Text="{Binding EditingStudent.Age, Mode=TwoWay}"/>

            <TextBlock Text="Gender" Margin="0,10,0,0"/>
            <TextBox Text="{Binding EditingStudent.Gender, Mode=TwoWay}"/>

            <TextBlock Text="Courses" Margin="0,15,0,5"/>

            <ItemsControl ItemsSource="{Binding AvailableCourses}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Course.Name}"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

                <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="80"
                    Margin="0,0,5,0"/>

                <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Width="80"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
