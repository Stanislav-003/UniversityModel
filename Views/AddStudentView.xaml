<Window x:Class="UniversityModel.Views.AddStudentView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UniversityModel.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddStudentView">

    <StackPanel Margin="10">

        <StackPanel>
            <TextBlock Text="First Name"/>
            <TextBox Text="{Binding NewStudent.FirstName}"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Last Name"/>
            <TextBox Text="{Binding NewStudent.LastName}"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Age"/>
            <TextBox Text="{Binding NewStudent.Age}"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Gender"/>
            <TextBox Text="{Binding NewStudent.Gender}"/>
        </StackPanel>

        <TextBlock Text="Courses:" Margin="0,10,0,5"/>

        <ItemsControl ItemsSource="{Binding Courses}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Course.Name}"
                              IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Margin="5"/>

            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Margin="5"/>

        </StackPanel>

    </StackPanel>
</Window>
