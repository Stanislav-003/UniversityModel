<Window x:Class="UniversityModel.Views.EditCourseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityModel.Views"
        mc:Ignorable="d"
        Title="EditCourseView" Height="450" Width="800">
    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Course Name"/>
        <TextBox Grid.Row="1"
                 Text="{Binding EditingCourse.Name}"
                 Margin="0,5,0,10"/>

        <TextBlock Grid.Row="2"
                   Text="Teacher"
                   Margin="0,5,0,0"/>

        <ComboBox Grid.Row="3"
                  ItemsSource="{Binding AvailableTeachers}"
                  SelectedItem="{Binding SelectedTeacher}"
                  DisplayMemberPath="FirstName"
                  Margin="0,5,0,10"/>

        <StackPanel Grid.Row="4"
                    Orientation="Vertical"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="0,10,0,0">

            <Button Content="Cancel"
                    Width="120"
                    Margin="0,0,10,0"
                    Command="{Binding CancelCommand}"/>

            <Button Content="Save"
                    Width="120"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>

    </Grid>
</Window>
