<Window x:Class="UniversityModel.Views.AddTeacherView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityModel.Views"
        mc:Ignorable="d"
        Title="AddTeacherView" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">

            <TextBlock Text="First Name"/>
            <TextBox Text="{Binding NewTeacher.FirstName}" />

            <TextBlock Text="Last Name" Margin="0,10,0,0"/>
            <TextBox Text="{Binding NewTeacher.LastName}" />

            <TextBlock Text="Age" Margin="0,10,0,0"/>
            <TextBox Text="{Binding NewTeacher.Age}" />

            <TextBlock Text="Gender" Margin="0,10,0,0"/>
            <TextBox Text="{Binding NewTeacher.Gender}" />

            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

                <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="80"
                    Margin="0,0,10,0"/>

                <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Width="80"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
